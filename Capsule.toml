[package]
name = "astrid-capsule-skills"
version = "0.1.0"
description = "Skills Loader - Reads and loads Agent Skills via VFS"
authors = ["Astrid Core Team"]
astrid-version = ">=0.1.0"

[[component]]
id = "skills-loader"
file = "astrid_capsule_skills.wasm"
type = "executable"
# Broad home:// read needed — callers pass arbitrary dir_path (e.g. ".gemini/skills")
# and the capsule scans both workspace and home trees under that path.
capabilities = { fs_read = ["workspace://", "home://"] }

[capabilities]
ipc_publish = []
